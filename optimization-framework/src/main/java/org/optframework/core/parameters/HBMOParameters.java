package org.optframework.core.parameters;

import org.cloudbus.cloudsim.util.workload.Workflow;

public class HBMOParameters extends ProblemParameters {
    public HBMOParameters(Workflow workflow) {
        super(workflow);
    }
}
